require 'rails_helper'

RSpec.describe AuctionsCleanupJob, type: :job do
  pending "add some examples to (or delete) #{__FILE__}"
  # Here i would want to test the job calls the service and enqueues the job if perform later
end
